N = int(input("Введите N: "))
K = int(input("Введите K: "))
sp = [x for x in range(0, N)]

index = -1  # Предполагаем, что убийства начинаются с K-го участника
while len(sp) != 1:
    index += K
    index = index % len(sp)  # Если номер участника превысил длину списка, вычитаем её, т. к. они стоят по кругу
    sp.pop(index)
    index -= 1  # Из-за выбывания трупа из списка нужно уменьшить индекс на 1

print(sp[0])
